# claude_test/ — Bench scripts and one-off diagnostics

This directory holds debug, exploratory, and diagnostic scripts per [CommonClaude/CLAUDE.md §3](https://github.com/coport-uni/CommonClaude/blob/main/CLAUDE.md). Scripts here are NOT part of CI/CD; production-quality tests live in [`tests/`](../tests/).

When adding a new file, append a row to the table below describing what it does and the key finding(s) that came out of running it.

| File | Purpose | Findings |
|---|---|---|
| [valve_toggle.py](valve_toggle.py) | Toggle the SY-01B valve between two distribution ports (default: 1 ↔ 3), verifying each move by polling `?6`. Runs `diagnose()` first; plunger never moves. | 2026-05-18 HIL on `/dev/ttyUSB1` (firmware 8.33): 20/20 moves verified, ~907 ms per port-to-port transition. Confirmed [LearnedPatterns.md](../LearnedPatterns.md) E5 (Q.busy unreliable post-init) and E6 (firmware treats MCC-4 as 4-way distribution; `?6` returns digits). |
| [syringe_init.py](syringe_init.py) | Run `diagnose()`, set stall current (`U200,<n>R` from `Config.syringe_uL`), then initialize plunger and valve (`Z<force>R`). Polls `?6` until it stops returning the literal `?` (per [LearnedPatterns.md](../LearnedPatterns.md) E7). Default force=2 (third) for the 125 µL bench syringe. | 2026-05-18 HIL on `/dev/ttyUSB1` (physical 125 µL syringe, empty): init at force=2 completes in **3.86 s** with post-init `?=0`, `?6='4'`. Verified across `--syringe-uL` ∈ {25, 50, 100, 125}: U200 operand correctly resolves to {4, 5, 5, 5}; all four runs identical 3.86 s init time (confirms U200 is EEPROM-only and current-session init uses the prior-power-up value — SY01BE.txt:1622). Surfaced [LearnedPatterns.md](../LearnedPatterns.md) E7 (the `?6 != "?"` completion signal). |
| [plunger_cycle.py](plunger_cycle.py) | After init, cycle the plunger through max (full stroke) → mid → min (= 0) absolute positions and verify each move by polling `?`. Default 3 cycles in NORMAL step mode (max=12 000, mid=6 000). Syringe must be empty; valve must not be in bypass. | 2026-05-18 HIL on `/dev/ttyUSB1` (physical 125 µL syringe, empty): **9/9 moves verified** across 3 cycles. Full stroke (0↔12 000) ≈ 3.26 s; half stroke (12 000↔6 000 or 6 000↔0) ≈ 1.6 s — consistent with post-init V=4000 pps default (surfaced [LearnedPatterns.md](../LearnedPatterns.md) E8). Valve stayed at port 4 throughout (default output port of a 4-way pump after Z init). |
